import{_ as a}from"./plugin-vue_export-helper-c27b6911.js";import{o as e,c as t,f as r}from"./app-2d139388.js";const i={},h=r('<h1 id="virtual-dom" tabindex="-1"><a class="header-anchor" href="#virtual-dom" aria-hidden="true">#</a> Virtual DOM</h1><h2 id="what" tabindex="-1"><a class="header-anchor" href="#what" aria-hidden="true">#</a> What</h2><p>虚拟 DOM 的<strong>工作原理</strong>是通过 JS 对象模拟 DOM 的节点</p><h2 id="why" tabindex="-1"><a class="header-anchor" href="#why" aria-hidden="true">#</a> Why</h2><p>在 Facebook 构建 React 初期时，考虑到要提升代码抽象能力、避免人为的 DOM 操作、降低代码整体风险等因素，所以引入了虚拟 DOM。</p><h2 id="how" tabindex="-1"><a class="header-anchor" href="#how" aria-hidden="true">#</a> How</h2><p>虚拟 DOM 在<strong>实现上</strong>通常是 Plain Object，以 React 为例，在 render 函数中写的 JSX 会在 Babel 插件的作用下，编译为 React.createElement 执行 JSX 中的属性参数。 React.createElement 执行后会返回一个 Plain Object，它会描述自己的 tag 类型、props 属性以及 children 情况等。这些 Plain Object 通过树形结构组成一棵虚拟 DOM 树。当状态发生变更时，将变更前后的虚拟 DOM 树进行差异比较，这个过程称为 diff，生成的结果称为 patch。计算之后，会渲染 Patch 完成对真实 DOM 的操作。</p><h2 id="优缺点" tabindex="-1"><a class="header-anchor" href="#优缺点" aria-hidden="true">#</a> 优缺点</h2><p>虚拟 DOM 的<strong>优点</strong>主要有三点：</p><ul><li>改善大规模 DOM 操作的性能</li><li>规避 XSS 风险</li><li>能以较低的成本实现跨平台开发。</li></ul><p>虚拟 DOM 的<strong>缺点</strong>在社区中主要有两点。</p><ul><li>内存占用较高，因为需要模拟整个网页的真实 DOM。</li><li>高性能应用场景存在难以优化的情况，类似像 Google Earth 一类的高性能前端应用在技术选型上往往不会选择 React。</li></ul>',12),n=[h];function c(l,o){return e(),t("div",null,n)}const u=a(i,[["render",c],["__file","Virtual DOM.html.vue"]]);export{u as default};
